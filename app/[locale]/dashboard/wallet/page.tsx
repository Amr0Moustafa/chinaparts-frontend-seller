import { WalletTemplate } from "@/components/template/WalletTemplate";


export default async function Wallet() {
  return (
    <section id="wallet-page" className=" min-h-screen ">
      <WalletTemplate/>
    </section>
  );
}