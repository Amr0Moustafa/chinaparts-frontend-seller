import ReserveDetails from "@/components/template/Reservedetails";

export default async function Reserve() {
  return (
    <section id="reserve-page" className=" min-h-screen ">
      <ReserveDetails/>
    </section>
  );
}